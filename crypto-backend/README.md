# Crypto Backend (Node.js + Express + MongoDB)

Quick scaffold for the crypto trading backend. See `src/` for implementation.
Run:
1. npm install
2. copy .env.example to .env and set values
3. node src/app.js
